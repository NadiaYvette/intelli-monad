# Wire-spike build outputs: every artifact here is regenerated by the
# run scripts from checked-in sources; nothing under these dirs is
# hand-written or needs versioning.
/build/
/build-koka/
/build-koka-gen/
/build-koka-mapped/
/build-multi/
/build-refused/
